---
description: Triage a Forge Harness repository issue using the project's own workflow.
---

# Project triage

Inspect the reported symptom, identify the affected checkpoint, and propose the smallest project-local next step.

This project skill intentionally shares the local name `triage` with the issue-workflow plugin. Invoke it as `/triage`.
